<!DOCTYPE html>
<html>
<head>
  <title>Your daily workout program</title>
</head>
  <body>
      <nav>
        <% if Helper.logged_in?(session) %>

          <a href="/exercises/new"> Create Exercise</a>
          <a href="/logout"> Log Out </a>
          <p>Welcome, <%=Helper.current_user(session).username %></p>
        
        <%end %>

      </nav>
    <div class="container">
      <% if !@error_message.nil? %>
      <div class="alert alert-warning">
        <%= @error_message %>
      </div>
    <% end %>
      <%= yield %>
      <footer class="branding">
      <small>&copy; 2017 <strong>WorkoutProgramApp</strong></small>
        </footer>
  </body>
</html>
